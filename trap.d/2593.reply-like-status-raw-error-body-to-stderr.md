Release audit (round 1, v0.62.0..HEAD delta, dispatch token 7ec1fb38d03a) found
the identical shape #2599 just fixed in `comment.py`/`auth.py`, freshly landed
by the sibling #2593 PR (`0ca6b6d8`, merged after #2599's fix `de88ba38`):

- `presets/youtube/reply.py:146` and `:155`
- `presets/youtube/like.py:106` and `:113`
- `presets/youtube/status_since.py:143`, `:150`, `:172`, and separately `:192`
  (also flows to stdout at `:205` -- "comments unavailable: {title} [{vid}]:
  {e}", rendered output rather than a terminal error path; found in round 2
  of the same audit, dispatch token 409586369e15)

All `sys.stderr.write(f"ERROR: {e}\n")` with a raw `OAuthError`/`YouTubeAPIError`
body -- the same `_yt._format_oauth_http_error` source #2599 rewrote to
`repr(safe_short(str(e), 300))` at four other sites. Neither issue #2593's
nor issue #2599's diff, taken alone, shows this; only the composed range
does, because the two PRs landed in parallel against the same file family.

`trap.d/2599.youtube-sibling-error-body-sites-still-raw.md` already tracks
`list.py`/`search.py`/`read.py` as remaining untouched siblings and is now
undercounted -- these seven sites belong in the same eventual sweep.

Ranked `misreports` (non-blocking) under the repo's own existing
classification for this error source ("Google's bytes, not a stranger's").
Open question worth settling before the next sweep: whether an API error body
can ever carry an operator-supplied string (e.g. an echoed comment id) -- if
so this reclassifies as `forges` and blocks.
