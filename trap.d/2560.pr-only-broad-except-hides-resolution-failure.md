Release audit of v0.61.0..HEAD (dispatch token 3322da2f18469846, round 1) found:
`presets/watch/sources/github-pr-feed/poller.py:203-212`, `pr_only()`. The whole
resolution is wrapped in `except Exception: _pr_only_cache = []`, cached for the
process lifetime. Docstring justifies this for a malformed `pr_exclude_events`
("radar already validated it once") -- but the bare `except Exception` also
swallows an `ImportError` loading the tier module, a missing `radar.py`, or a
`read_tiers()` decode failure, none of which radar validated. On any of those,
every per-PR poller this feed forks gets no event filter at all, silently and
permanently, and `terminal_coverage(spawned=True)` (line 359) then reports
`only = pr_only()` (i.e. `[]`) as a *known* filter rather than "could not tell".
Same `[]` renders for "operator configured no exclusions" and "resolution
failed" -- no diagnostic on the except path either way.

Row: misreports (ranking table). Non-blocking. Fix: narrow the except to what
radar actually validates (malformed `pr_exclude_events` value), or have the
except path record a distinct could-not-resolve state `terminal_coverage` can
read and report as unknown rather than as an empty, deliberate exclusion list.
