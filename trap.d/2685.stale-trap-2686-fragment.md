Issue #2685's own self-review (oss:auditor round, on commit e488ba57) found that
`trap.d/2686.claude-log-trunc-unflattened.md` -- the standing trap fragment
that named this exact defect (`trunc()`'s bare CR/LF-only replace in
presets/claude-log/_common.py, and #2686's scanner not covering
presets/claude-log) -- is now stale. #2685's fix routes `trunc()` through
`_untrusted.flat()` and adds `presets/claude-log` to
tests/test_bare_separator_replace_2686.py's `_SCANNED` tuple, which answers
both of that fragment's open questions (line 5's "does NOT scan
presets/claude-log" and lines 30-37's "whether this is worth promoting to a
real fix") in the affirmative.

The fragment itself was left untouched by #2685's diff -- deliberately, since
deciding whether to delete/amend an existing trap fragment versus leaving it
for the curation pass is a curation decision, not something a lane fixing the
underlying defect should decide on its own. Ranked `misreports` (non-blocking,
"can ship behind a trap.d fragment") by the auditor, same as the original
fragment's own ranking.

What would settle it: whether trap.d/2686.claude-log-trunc-unflattened.md
should be deleted outright (its defect is fixed and both open questions are
answered) or left for /oss:curate to process as any other fragment -- no
stated convention for "a fragment resolved before curation runs" was found
in trap.d/README.md or docs/contributing.md during #2685's own review.
