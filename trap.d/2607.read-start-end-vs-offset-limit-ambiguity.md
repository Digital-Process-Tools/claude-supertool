While working #2607, a request for `read:CHANGELOG.md:1:90` (intending "lines 1 through 90")
instead windowed as skip-90-then-return-90 -- `read`'s positional args are `OFFSET:LIMIT`, not
`START:END`, and nothing at the call site distinguishes the two readings until the op's own
returned hint syntax corrects it. Cost one round-trip on the developer lane, and recurred
independently for the report-writer sub-agent reading the agent-report schema file in the same
run -- two unrelated callers making the identical wrong assumption in one lane.

Not fixed here: whether the op should warn when the second argument looks like a line number
larger than a plausible LIMIT (e.g. flagging `1:90` as likely-START:END when OFFSET=1), or
whether the fix is purely documentation (the op's own `description` making OFFSET:LIMIT more
prominent), is a design question outside #2607's scope. Ranked non-blocking (a round-trip cost,
not incorrect output -- the op's own hint syntax self-corrects on the next call) -- logged here
rather than filed, per the ranking table's "can ship behind a trap.d fragment" row.
