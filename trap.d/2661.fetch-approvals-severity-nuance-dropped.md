Self-review on #2661 (oss:auditor pass) flagged that the `_fetch_approvals()` jit-context
section added by PR #2660's curate commit (folding `trap.d/2645.approvals-lookup-failure-not-
surfaced.md` into `.claude/jit-context/paths/00-manual/presets-watch.md`) dropped a
severity-narrowing paragraph the original trap.d fragment carried: "This is narrower than it
first looks... the confirming request now fires on at most one tick per genuine transition... a
failure here is bounded to that one tick, not an ongoing silent outage."

The jit-context copy keeps the bug description and the "mirror mr_unreachable" fix pointer but
not that bound. A reader of the jit rule alone, with no access to the now-deleted trap.d file,
cannot tell this is a bounded, low-frequency gap rather than a standing outage risk.

Not fixed inline here: the content is PR #2660's own curate decision (already a reviewed,
independent lane), and #2661's own scope is the byte-budget trim, not re-litigating #2660's
section wording. Whether the severity bound belongs back in the rule, in a shorter form, is a
call for whoever curates this fragment next -- not decided here.
