While working #2607, the raw-write jit guards (`python-heredoc-writes-are-unvalidated.md` and
its `cat >` sibling) correctly blocked two independent raw-write attempts in the same lane run:
the developer building a TOML edit payload, and the report-writer sub-agent writing its own
note. Both routed cleanly to `edit:@-`/`paste:@-` on stdin on the first retry.

This is not a defect -- the guards did their job and the retry cost was one call each. Logged
here only because it is the kind of near-miss this repo's own doctrine (CLAUDE.md: "when you
reach for a workaround, that's the signal") asks to be said out loud, and because two
independent trips in one lane is a data point on how often the raw-write reflex still fires
despite the guard's rule text. No action needed unless this recurs at a rate that suggests the
guard's own prose isn't landing.
