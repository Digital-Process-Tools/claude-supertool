Release audit of v0.61.0..HEAD (dispatch token 3322da2f18469846, round 1) found:
`presets/watch/sources/gh-branch/poller.py:403-412`, the `#2537` streak message.
`raw_needs_guard = raw_is_no_run or raw_is_unread_jobs` folds two independent
failure boundaries into one counter -- the comment right above it (lines 392-401)
already says a homogeneous claim can't be backed by this counter. The
`raw_is_unread_jobs` arm was reworded to "most recently because...", but the
`else` arm still unconditionally reads "N consecutive polls ... have now come
back with an empty run list, most recently.". A streak of [job-list miss, empty
run list] hits the else arm and tells the operator both polls returned an empty
run list -- one didn't. Sends the operator to `gh run list` when the failing
call was actually `gh run view --json jobs`.

Row: misreports (ranking table). Non-blocking. Fix: the `else` arm needs the
same "most recently because..." treatment the `raw_is_unread_jobs` arm got, or
the two counters need to stop sharing one message entirely.
