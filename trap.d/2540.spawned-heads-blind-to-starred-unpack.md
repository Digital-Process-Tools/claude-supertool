Release audit of v0.61.0..HEAD (dispatch token 3322da2f18469846, round 1) found:
`tests/test_spawnable_register_2540.py:52-70`, `_spawned_heads`. Ran the walker
over the tree: 58 adapter sources, offenders: none -- the register is a real
guard with a positive control and a population floor. But it records
`ast.unparse(node.elts[0])`, so a list literal headed by a `Starred` or a `Name`
never matches a `which()` argument string. `formatters/prettier-write/prettier-
write.py:89` is exactly that shape -- `cmd = [*bin_cmd, "--write"]`, unparsing
to `*bin_cmd` -- and is invisible to the register. It's correct today only
because `resolve_bin_cmd` was independently fixed in the same delta
(validators/common/bin_resolve.py:46-68), not because the register saw it.

Row: misreports (ranking table, "a green register that does not cover the
formatters it walks"). Non-blocking. Fix: extend `_spawned_heads` to resolve a
`Starred`/`Name` head against the variable's own assignment, or explicitly
document the shape it can't see so a future reviewer doesn't trust it further
than it reaches. Related, not itself a finding: `bin_resolve._spawnable` and
`spawnable.argv0` are now two copies of the same eight lines and the same 3.12
shutil.which docstring -- a drift surface this register also does not watch.
