While extending #1955's write-through mirror to `gh-pr` (#2472), the oss:auditor + Explore
self-review pair caught a bare `mirror_number.isdigit()` freshly introduced into
`presets/github/pr.py`'s new mirror-write gate (line ~1006) -- the file already imports
`_digits` and uses `_digits.is_ascii_int(arg)` everywhere else in the same function for
exactly this reason (#1727: `str.isdigit()` is `True` for non-ASCII Unicode decimal digits
like `٢`, so it does not actually reject the class of input the "never trust a non-numeric
API-reported number into a path" defense exists to catch). Fixed in #2472's own commit by
swapping to `_digits.is_ascii_int(mirror_number)`, and confirmed red-then-green against
`tests/test_ascii_digit_guards_1727.py::test_no_guarded_file_reaches_for_isdigit_again`
(`presets/github/pr.py` is in that test's `GUARDED` tuple; this call site's addition made it
fail before the fix).

**Left unfixed, deliberately, and reported here instead of patched:** `presets/github/issue.py`
has the IDENTICAL flawed pattern at its own mirror-write gate (`mirror_number.isdigit()`,
introduced by #1955, unmodified by #2472's diff), and it is caught by nothing -- `issue.py`
is not in `test_ascii_digit_guards_1727.py`'s `GUARDED` tuple, so this instance renders green
on every CI run. It also does not currently import `_digits` at all, so fixing it is a new
import plus a one-line swap, in a file #2472's own diff never touched -- outside that lane's
blast radius by the letter of "in_blast_radius", even though it is the exact same subsystem
and the exact same one-line fix. Worth someone deciding: fix `issue.py` to match, and decide
whether to add `github/issue.py` to `GUARDED` at the same time (it would have caught this
class the day #1955 shipped).
