`.claude/jit-context/paths/00-manual/go-warmup-lock-hot-file.md` names #2563 by number as
"a further residual" surfaced by #2559's merge -- that residual is fixed by this lane's commit
(the write-probe in `_lock_dir_usable` now retries once on a transient `OSError` before
concluding `lock_dir` is unusable). The rule's own advice ("read the whole function fresh rather
than just the diff's own hunk") was followed here: the whole function was re-read, and no further
residual was found in this pass, but the file's own history (four independently-correct fixes in
one day, each surfacing the next) is reason enough not to claim this is the last one.

Worth a curation decision: does the rule's body need updating to say #2563 landed, or is leaving
it as a standing warning (rather than a closed-issue list) the more durable form? Not decided here
-- logged for `/oss:curate`.
