Release audit of v0.61.0..HEAD (dispatch token 3322da2f18469846, round 1) found:
`presets/youtube/_oauth.py:230-236,267`. `_free_loopback_port()` binds port 0,
closes the socket, returns the number; `authorize()` then binds
`http.server.HTTPServer(("127.0.0.1", port), ...)` at line 267. In the window
between the two calls another local process can take the port, and the
HTTPServer constructor raises OSError. `auth.py:main` only catches OAuthError
(line 77), so the operator gets a raw traceback instead of the sentence-naming-
the-next-command contract the module docstring promises for "every failure
path". (The race itself is benign -- a hijacker can't pass the `state` check at
line 288 -- this is purely the error-arm gap, not a security issue.)

Row: misreports (ranking table). Non-blocking. Fix: wrap the HTTPServer
construction in `authorize()` with an except OSError that reports the port
conflict the same three-state way every other failure in this module does.
