Lane #1595's `edit:@-` call could not match an old/new substring pair against this issue's own changelog fragment, even though the target substring was visually present in a plain `read` of that exact file -- a single-line bullet, no embedded newlines. Fell back to a full-paste rewrite instead of a targeted diff-sized edit.

Root cause undetermined at the time: possibly a literal-backslash-in-payload interaction carried over from an earlier write in the same session, or whitespace at a wrap boundary that `read`'s rendering does not distinguish from the byte-for-byte content `edit:@-` matches against.

Not a defect in the code the lane was reviewing -- reported by `oss:lane-report`'s own `adjacent` block as `report-for-filing`, `in_blast_radius: false`. Ranked non-blocking: it cost the lane one avoidable full-file rewrite rather than a small diff, not a wrong result, a security issue, or a merge-blocker. Logged here rather than filed, per the ranking table's "can ship behind a trap.d fragment" row -- worth reproducing with the exact payload and file state the next time `edit:@-` silently misses a visually-present match.
