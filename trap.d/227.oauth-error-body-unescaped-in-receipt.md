Release audit of v0.61.0..HEAD (dispatch token 3322da2f18469846, round 1) found:
`presets/youtube/_yt.py:117-131` (`_format_oauth_http_error`) reaching
`presets/youtube/comment.py:196`. On the could-not-verify path, `verify()`
returns `str(e)` from `YouTubeAPIError`, embedding up to 300 bytes of the HTTP
error body unescaped and un-truncated for newlines; `comment.py:196` renders it
via `print(f"read-back: {check_verdict} -- {detail}")`. A body containing a
newline puts the remainder at column 0 of a receipt the calling agent parses.
The MISMATCH arm right next to it does this correctly -- `safe_short(got,
120)!r` at comment.py:149 reprs the server text -- so the two arms of the same
function disagree. Graded misreports rather than forges: the text originates
with Google's API, not a stranger who chose the bytes deliberately -- if a
reviewer can show a third party controlling that body, this becomes a `forges`
row and blocks.

Row: misreports (ranking table). Non-blocking. Fix: repr()/escape the body in
the could-not-verify arm the same way MISMATCH already does.
