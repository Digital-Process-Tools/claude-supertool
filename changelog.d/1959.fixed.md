- **`gh-branch` (and `presets/dashboard/dashboard.py`, the board a release is tagged against) could call a fresh commit `NOT GREEN` for a workflow its own footer, in the same render, said could not have produced a run at all** ([#1959](https://github.com/Digital-Process-Tools/claude-supertool/issues/1959)). Inside the ~15min creation window the verdict treated "ran on the previous head, absent here" as evidence a run was still owed, without consulting the trigger analysis the same render already prints below it — so a `schedule`/`workflow_dispatch`-only workflow (`slow tests` on this repo) held the verdict at `NOT GREEN` on every push, for the whole window, while the footer said in the next paragraph that no push trigger reaches it and none of it is covered. `missing_workflows` now takes the declared-at-this-sha workflow set and drops a name from its result only when the trigger set **proves** a push could not have produced a run — a workflow with no push trigger, kept when the trigger set is unreadable or genuinely absent, and never dropped when a push trigger is declared, which is the control this fix must not destroy: a workflow a push *should* reach that produced no run still holds the verdict, still waiting, exactly as before.
