- **The render-size claims registry now discovers `docs/operations/meta.md` instead of needing every claim in it added by hand** ([#2058](https://github.com/Digital-Process-Tools/claude-supertool/issues/2058)). `tests/test_render_size_claims_1877.py`'s `SITES` list only graded a claim if someone had already added a row for it, and `docs/operations/meta.md` sat outside `REGISTERED_FILES` entirely -- four separate instances on the issue thread found a stale `ops`, `ops:full` or `ops-compact` figure there that no test could ever have gone red about. `_render_doc_files` now lists `docs/operations/*.md` at test time and feeds every file it finds, not just the ones already in `SITES`, into the same ungraded-figure sweep -- a new file dropped into that directory tomorrow is covered from the day it exists. `docs/operations/meta.md`'s four claims are graded now (one needed a pattern anchored to its own table cell, since the file also uses the bare word "`ops`" in a dated aside and a borrowed mention of `ops:full`'s old size). The merge-conflict hazard from a figure duplicated across every claim site (recorded on the issue thread) is not addressed here and stays open.
