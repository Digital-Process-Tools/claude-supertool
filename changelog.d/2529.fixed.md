- **`watches` names a foreign channel's own plugin-cache version, against what is installed now** ([#2529](https://github.com/Digital-Process-Tools/claude-supertool/issues/2529)). A poller forked from an old plugin-cache copy kept running that copy's code indefinitely, and the disclosure of another channel's fleet (`the process scan also saw N labelled poller(s)...`) said only a channel hash and a state dir. The version was already sitting in `ps`'s own argv (a poller's own `dispatcher.py` path names the plugin-cache directory it forked from), so the line now reads e.g. `4 on channel da940576c962, 1 slot(s) -- state dir ..., running supertool 0.57.0 (installed: 0.60.0)`. Disclosure only, same as the rest of this render -- no restart is offered or taken across channels.
