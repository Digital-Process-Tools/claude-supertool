- **Four renders no longer collapse a path's newline to a space, which could name a different, plausible, and possibly existing file** ([#1571](https://github.com/Digital-Process-Tools/claude-supertool/issues/1571)). `presets/_untrusted.flat`'s `disclose_newline=True` exists precisely so a path a reader has to be able to identify again on disk is not rendered with a lie -- the default collapse is right for a title, which cannot hold a newline on either tracker, and wrong for a path, where the space produces a name that is not on disk. `12c3a8f` set the flag for `git-worktrees`' own banner line; `_path_not_found`, `op_edit`'s success line and the dispatch header kept the title-shaped default, and `git-worktrees`' board did the same for `entry['path']` and the `wanted` argument disclosed one line above it in the same listing -- two spellings of one path in one render. All five now disclose.
