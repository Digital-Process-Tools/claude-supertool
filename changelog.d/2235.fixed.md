- **`test_gl_repo_target_676.py`'s env-leak test flakes on Windows: its nested `pytest` child dies at collection with `FileNotFoundError: [WinError 2]` naming a bare temp directory that is neither the test's own private `child_tmp` nor anything this repo's code creates.** The #2015 mitigation (redirecting the child's own TMP/TEMP/TMPDIR) already narrows the test's *own* temp usage; it cannot stop an unrelated program on the same CI runner from racing the shared system temp root while the child starts up. The test now retries only a harness death -- an exit code meaning the child never produced a verdict at all (`_nested_pytest_verdict.is_harness_death`, `run_with_harness_retry`) -- and never a real product disagreement (exit 1), so a retry can recover from or reconfirm a harness hiccup but can never mask a genuine regression. Reasoned, not observed: traced from a Windows CI log (run 33827843384, job 100884282945), never reproduced on this platform; the exact failure text was reproduced locally by simulating the same subprocess.run() sequence. [#2235](https://github.com/Digital-Process-Tools/claude-supertool/issues/2235)
