- **The two bare-spawn registers (`tests/test_gate_matches_spawn_2579.py`, `tests/test_spawnable_register_2540.py`) now catch what their own assertion text already claimed to cover** ([#2606](https://github.com/Digital-Process-Tools/claude-supertool/issues/2606)). `_bare_existence_check_without_dirname_guard()` previously missed a bare-imported `isfile(X)` spelling and an existence check with no surrounding `and`-chain at all, and false-flagged a `dirname()` guard nested inside `bool(...)`; it now walks up to the nearest enclosing `and`-chain via a parent map and correlates the checked name against what a chokepoint call (`spawnable()`/`argv0()`/`resolve_bin_cmd()`) actually resolves, so an adapter's unrelated file-existence check (does `Cargo.toml`/`go.mod` exist?) is no longer a false positive. Both registers' own assertion text now states the scope it does not cover -- a bare, entirely ungated argv[0] literal is `tests/test_bare_argv0_construction_2605.py`'s job instead.
