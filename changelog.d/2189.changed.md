- **README.md links out to the subsystems the ops table only names.** #2142 cut the front page from 825 to 167 lines and left "validators, formatters and notifiers" as three unlinked nouns in the opening sentence, and `docs/mcp-warm-process-servers.md` reachable from nowhere at all. A new "Beyond the ops table" block (one sentence and a link each for validators/formatters, notifiers, `watch`/`radar`/`channel`, and the LSP/MCP daemon) plus links added directly on the matching ops-table rows fix that without regrowing the file `#2142` just cut -- `docs/presets/index.md` already covers the preset pages two hops out and is untouched. `tests/test_readme_pitch_shape_2142.py::test_every_docs_file_is_reachable_from_readme_or_explicitly_unlinked` walks the local-link graph from README.md and pins that every file under `docs/` is now reachable by some chain of links, so a future doc landing with no path in stays red instead of drifting. [#2189](https://github.com/Digital-Process-Tools/claude-supertool/issues/2189)
