- **Two registry-vs-code token disagreements from the `#1269` sweep, pinned rather than fixed.** `gl-pipeline:ID:full` dispatches (`_FILTERS` accepts it) but the op's own refusal text and `presets/gitlab.json`'s `syntax` both only ever offer `active`/`failed`/omission -- fixing either side means editing `presets/gitlab/pipeline.py` or `presets/gitlab.json`, both held by a concurrent lane at the time this was worked. `mcp_stop:--all` reaches the identical stop-everything code path as the declared `mcp_stop_all` op, with no signal at the script boundary to tell the two callers apart -- a code fix gating `--all` behind an op-specific signal would have broken `tests/test_mcp_stop_pid_zero_569.py`'s own direct exercise of that shared path. Both are pinned with tests naming them undecided rather than intended, per the issue's own closing line. [#1315](https://github.com/Digital-Process-Tools/claude-supertool/issues/1315)
