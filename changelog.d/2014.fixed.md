- **`stalled_at_its_own_wall`'s mixed-shape decline message -- wall count and total distinguished -- is now pinned, not just its verdict** ([#2014](https://github.com/Digital-Process-Tools/claude-supertool/issues/2014)). `test_a_wall_beside_a_finding_is_not_a_verdict` only asserted the payload declines; #1709's own "N of its M reported errors are that wall" clause could be reworded to a single count, or made to report the same number twice, and no test would go red. `test_the_message_names_both_counts_distinctly` extracts both numbers with a structural regex tied to their roles (wall count vs. total) rather than a bare substring check on one digit -- which a single-count message would also satisfy -- and asserts they are present, correctly attributed, and distinct. Verified red by mutating `tests/_adapter_verdict.py:340` to a one-count message, then reverted.
