- **This repo's own changelog gate is replaced by the oss plugin's owned one** ([#2489](https://github.com/Digital-Process-Tools/claude-supertool/issues/2489)). `.github/workflows/changelog.yml` and `.github/scripts/assemble_changelog.py` (1164 lines) are gone; `.github/workflows/oss-changelog.yml` and `.oss/assemble_changelog.py` (3049 lines, `/oss:scaffold`-owned) take over, adding a `user_visible_paths` read out of `.oss.json` and a dependabot exemption that the local gate never had. `/oss:scaffold` can reach this repo again.
