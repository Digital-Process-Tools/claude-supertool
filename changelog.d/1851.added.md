- **`gh-pr-merge` now reports whether a stacked follow-up is left unwatched after the merge** ([#1851](https://github.com/Digital-Process-Tools/claude-supertool/issues/1851)). The receipt covered the merged PR itself — state, mergedAt, mergeCommit, linked-issue closure, base-branch drift, default-branch health, cleanup — but said nothing about the board fact only the merging op can establish at merge time: whether a second open pull request now targets the branch just merged, which a downstream watcher-healing loop (`claude-oss`, on every board-membership change) cannot re-derive without a second full board read that costs more than the heal it was trying to avoid. A new `## Stacked follow-up` section and `stacked_followups()` answer in three states — a follow-up is named, none exists, or `unknown` when the one added `gh pr list --base <head>` call itself could not be read — never collapsing "could not tell" into "none", and the `[result]` line's tail now names the same state. Skipped, not searched, when the merge itself is unconfirmed. Filed from `Digital-Process-Tools/claude-oss` (claude-oss#302), where this op sits on the merge path of every tick.
