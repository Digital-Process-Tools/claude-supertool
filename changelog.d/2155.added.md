- **A new file's unused import is now caught locally, not only by CI's added-path lint leg.** `pyproject.toml` ignores F401/F841/F541 tree-wide for 263 pre-existing findings (#797); `.github/scripts/lint_new_files.py` re-enables them for the files a PR adds, copies or renames, but only in CI, against a PR base ref no local route has. Measured cost: three separate lanes shipped a new test file with a dead import in one session, each green on every local check and red on the CI leg after a full push-and-wait round trip. `validators/new-file-lint/new-file-lint.py`, wired into `.supertool.json` as the `new-file-lint` validator, states no rules of its own -- the same shape `validators/changelog-fragment/changelog-fragment.py` already uses for `assemble_changelog.py` -- and instead walks up from the target file looking for the project's own `lint_new_files.py`-shaped script (default `.github/scripts/lint_new_files.py`, overridable via `SUPERTOOL_NEW_FILE_LINT_SCRIPT`), importing its `EXTRA_RULES` rather than hardcoding a ruleset for every consumer of this plugin. It asks the one question a local, PR-less check actually can answer -- does this path have a blob at `HEAD` -- and re-enables those rules for any `.py` file that does not, on every mutating op; a file that already has history is left to the shared `ruff` validator and the CI leg's own merge-base diff, which this adapter has no PR base ref to reproduce. **What it does not close**: it is a lint gate, not a census. It would not have caught either of #2196's own two red CI legs -- `test_every_configured_validator_cmd_is_an_adapter` (this validator's first cut lived outside `validators/` and pointed at a raw, repo-hardcoded script instead of a discoverable adapter) and `test_symlink_gating_register_1232.py`'s call-site register (a new `symlink_to()` in this gate's own test suite) -- because neither is a ruff finding. [#2155](https://github.com/Digital-Process-Tools/claude-supertool/issues/2155)
