`bin/supertool-workspace` now registers the channel consumer itself, at local scope with an absolute path, instead of tagging `claude-channel` from the plugin's `.mcp.json` — whose `${CLAUDE_PLUGIN_ROOT}` is empty outside a plugin load, so that server never spawned and the workspace armed itself against nothing while `radar` rendered a normal board (#1544).
