- **The `notifiers` CI job now runs `channel.ts` under the exact node command real users launch** ([#2469](https://github.com/Digital-Process-Tools/claude-supertool/issues/2469)). `.mcp.json`/`start.mjs` have shipped `node --experimental-strip-types` since #520/#2486, but the job only ever type-checked and exercised `channel.ts` under bun -- no CI leg had ever spawned the exact runtime real users run. A new "Run channel.ts under node for real" step installs node 22 and re-runs a real MCP handshake plus the #550 stale-socket-reclaim scenario against the node-specific `ECONNREFUSED` branch of `channel.ts` own `probe()`, with a real subprocess spawn rather than only `tsc --noEmit`. The bun leg is unchanged -- `bun.lock` and the job cache key stay bun-keyed, per the scope decision #520 own review made.
