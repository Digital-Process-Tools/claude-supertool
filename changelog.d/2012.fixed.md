- **`markdownlint` fired 846 findings across 50 of 82 tracked `.md` files, and nearly all of it was genre noise rather than a defect** ([#2012](https://github.com/Digital-Process-Tools/claude-supertool/issues/2012)). `.markdownlint.json` now also disables MD040 (unlabeled fences over pasted op output), MD038 (deliberate quoted spaces in code spans), MD033 (angle-bracket CLI placeholders and README's banner HTML), MD056/MD052 (a naive table/reference parser that does not know a `|` or `[...]` inside a backtick code span is not a delimiter), MD024 (Keep a Changelog's own repeated section headings), MD025 (`.claude/jit-context/**` rule bodies use repeated `#` headings as section markers by design), and MD014 (inline `# result exit 0` comments, which do show the output the rule cannot see). MD031, MD022 and MD018 stayed on and were fixed at the source instead — the 8 MD018 hits were all a hard-wrapped issue reference starting a line (`#1426` at column 0), fixed by escaping the hash rather than disabling a rule that still catches a genuine malformed heading. Repo-wide findings: 846 → 26, pinned by `tests/test_markdownlint_noise_2012.py`.
