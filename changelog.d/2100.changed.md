- **`gh-pr-create`, `gh-pr-edit`, `gh-issue-create` and `gl-issue-create` never carried the #2042 AI-authorship marker, so which of this loop's own pull requests and issues disclosed a machine wrote them depended on which lane happened to think of it** ([#2100](https://github.com/Digital-Process-Tools/claude-supertool/issues/2100)). `presets/_publish_safety.apply_disclosure`, built for a fixed, freshly-composed body under a 300-character cap (Slack, Bluesky), never reached the forge ops. A new `apply_forge_disclosure` covers them instead of reusing `apply_disclosure` outright, because two things differ: there is no length cap, so the `dropped` state has no analogue, and `gh-pr-edit` republishes a whole body read back from the forge, so the marker has to be idempotent -- a body that already carries it reports `"already-present"` rather than stacking a second copy. `gh-pr-create` applies it after parsing the body for `Closes #N`, never before, so the marker cannot interfere with that gate. Issue and merge-request comments are unchanged; the issue named them a separate, undecided question.
