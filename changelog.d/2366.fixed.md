- **`presets/_publish_safety.py`'s own `.supertool.json` walk now carries the same trust boundary the core loader and three sibling walkers already had** ([#2366](https://github.com/Digital-Process-Tools/claude-supertool/issues/2366)). `_supertool_config` re-implemented an upward filesystem walk with no `.git`-ancestor stop and no ownership/permission check before trusting a config file it found -- the pre-#695 gap the core loader (`_supertool._load_config`), `presets/gitlab/_maintenance.py` (#2365) and `presets/worktree/_common.py` (#2370) were each already fixed to close, and `presets/slack/_authorization.py` closed independently (#2416) while this issue was open. `_publish_safety.py` feeds `require_confirm`, `_body_allowlist` and `_disclosure_config` for the social-publish presets (`bluesky`/`devto`/`hashnode`), so an unbounded walk with no ownership check meant a config placed anywhere above cwd by a less-trusted local account could be picked up for a publish-credential-adjacent decision. Fixed with the same `_config_trust_violation` shape the three already-hardened copies use -- presets cannot import the core module, so each walk-up loader re-implements the check rather than sharing it, which is also how this gap and #645's independently-reintroduced copy of it both arose; a shared helper across all four copies is a real follow-on (`presets/worktree/_common.py`'s own PR already suggested it) but was left out of this diff's blast radius since three of the four walkers are already independently hardened and passing, and a repo-wide refactor with no new bug to justify it is a different, larger change.
