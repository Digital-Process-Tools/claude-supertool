- **`radar`'s description in `presets/watch.json` is at the #1774 size ratchet, so a new tier can no longer document itself there** ([#2367](https://github.com/Digital-Process-Tools/claude-supertool/issues/2367)). `radar` is one of the twelve ops in `tests/test_description_is_not_a_changelog_1774.py`'s `_OVER_BUDGET` ledger, already over `MAX_DESCRIPTION` and allowed only to shrink; adding even one sentence about a newly-registered tier fails `test_the_over_budget_ledger_only_ever_falls`, which is exactly what happened once during #898's `gl-issue` tier and would recur with every future one. Documented rather than reworked: a new `.claude/jit-context/paths/00-manual/watch-json-description.md` rule fires on any edit to `presets/watch.json` and points a new tier's documentation at `docs/presets/watch.md` instead, and that page now carries a matching "Document a new tier here, not in `radar`'s own description" section next to its per-tier walkthrough. No change to the ratchet itself, or to any op's description.
