- The jit rule pointing `gh pr view` and `gh pr merge` at their ops now covers `gh pr create` too. `gh-pr-create:@FILE` had existed the whole time — it requires `base` rather than defaulting it, says whether a check actually started, and parses the body closing references at creation — and three raw calls still went out in one session because nothing said so at the moment of typing. (#1265)
