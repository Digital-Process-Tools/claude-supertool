- `grep`'s `0 results` output now names case-insensitivity when the pattern DOES match case-insensitively, so a write confirmed with a differently-capitalised grep no longer reads as a failed write (#1777).
- `read:PATH:OFFSET:LIMIT`'s disclosure of which form was read -- and how to get the other one -- is now also repeated at the foot of a long window, not only above the content, so it does not scroll off before the caller reaches the lines it corrects (#1777).
