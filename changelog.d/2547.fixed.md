- **The #150 ReDoS backtracking guard now applies on the `around` and `read:...:grep=` paths, not only `grep`** ([#2547](https://github.com/Digital-Process-Tools/claude-supertool/issues/2547)). `_has_outer_wrapped_unbounded_group` (#1311, hardened by #2535/#2546) was inlined only inside `_op_grep`. `_pattern_gate` -- the single chokepoint `around`'s public wrapper and `read`'s `grep_filter` both route through -- never called it, so the same adversarial pattern (`(x(a+)+y)`) reached a live `re.compile`/match against real file content on both of those routes completely unrefused; the #2535 lane's recon confirmed this live against `_op_around`. The guard is now single-sourced inside `_pattern_gate`, ahead of the BRE rewrite, so every route through it inherits the refusal instead of each op re-deriving it. `tests/test_pattern_gate_backtracking_guard_2547.py` is the regression coverage.
