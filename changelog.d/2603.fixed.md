- `which_excluding_cwd()`'s three copies (`validators/common/spawnable.py`, `presets/_spawnable.py`, `_supertool.py`) reported a genuinely installed tool as absent whenever the caller's `PATH` was unset (a stripped `env=` on a parent spawn, some cron/launchd contexts) rather than deliberately empty. `shutil.which()` itself falls back to `os.confstr("CS_PATH")`/`os.defpath` when `PATH` is unset, and only refuses on an explicitly empty `PATH=""` (bpo-35755); all three copies now mirror that distinction instead of collapsing both cases ([#2603](https://github.com/Digital-Process-Tools/claude-supertool/issues/2603)).
