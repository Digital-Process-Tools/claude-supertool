- **The always-injected CI rule named three of `tests.yml`'s four jobs** ([#2216](https://github.com/Digital-Process-Tools/claude-supertool/issues/2216)). `lint-new` -- the #1481/#1849 ruff-over-the-diff gate -- was missing, so a red leg named `lint (files this PR adds, renames or modifies)` was a job the rule said did not exist, and its "Reading a red leg" section sent the reader to `junit_summary.py`, which has nothing to say about a ruff failure. The two `tests.yml:N` citations had also moved (117 to 239) and are now made by job and step name, which survive a rebase.
