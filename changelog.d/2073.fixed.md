- **Give the `classify` live controls (#2046) a countable skip** ([#2073](https://github.com/Digital-Process-Tools/claude-supertool/issues/2073)): `tests/test_classify_live_2046.py`'s four tests always skip on the scheduled `slow tests` runner -- nothing installs or authenticates `claude` there -- and until now that skip carried no token, so the suite's own skip census reported zero against every reason it knew while all four went unaccounted for. `tests/_classify_live.py` is the `_live_gh.py`-shaped fix: a shared `require_claude()` that skips carrying `classify-live(#2073)`, counted by `conftest.py`'s terminal summary at zero and at the full population alike, with wording that states this tier's expected count is either 0 (a runner with `claude` on PATH) or the full population (every scheduled runner today) rather than always 0.
