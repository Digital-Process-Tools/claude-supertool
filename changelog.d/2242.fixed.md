- **`docs/validators.md`'s trust-boundary section now describes both `new-file-lint` / `changelog-fragment` gates** ([#2242](https://github.com/Digital-Process-Tools/claude-supertool/issues/2242)). It still described only the substitution gate (`_config_dir_is_untrusted_ancestor`, ancestor-only, unchanged since #2228) and claimed a disjoint or sibling `config_dir` was "never" refused — false since [#2241](https://github.com/Digital-Process-Tools/claude-supertool/pull/2241) added the stricter execution gate (`_config_dir_may_authorize_execution`, ownership-only) that refuses exactly that shape.
