- **`claims` declares its safety class, and master is green again** ([#1238](https://github.com/Digital-Process-Tools/claude-supertool/issues/1238)). The op shipped without the `"safety"` key that [#1243](https://github.com/Digital-Process-Tools/claude-supertool/issues/1243) began requiring of every preset op; the two PRs merged 21 seconds apart, each green against a merge base where the other did not exist, and the pair was red. `claims` is now declared `read-only`, which is what it is rather than what unreds the test: it reads the document, reads manifests off disk, and shells out to `gh issue view` / `gh repo view` / `git rev-parse` — every reference lens is a read, the issue-state cache lives in memory for the length of the call, and nothing is written anywhere. `docs/presets/claims.md` already opened with "One op, read-only, no writes anywhere"; the manifest simply never said so, and an undeclared op renders `!` in `ops:roster` — over-marked, by design, so the gap was loud rather than quiet.
