- **Two statusline tripwires reddened by the claude-oss 0.26.0 scaffold now assert the guarded state instead of the gap it closed** ([#2350](https://github.com/Digital-Process-Tools/claude-supertool/issues/2350)). `#2299` had repointed `tests/test_statusline_api_ref_validation_2245.py` and `tests/test_statusline_repo_only_api_ref_validation_2278.py` at `.oss/statusline.py`'s then-unguarded `gh api` call sites, filed the gap upstream as `claude-oss#1035`, and said in the docstring this was meant to fail loudly the day a guard was reintroduced. `#2349`'s re-scaffold onto claude-oss 0.26.0 did exactly that -- `_malformed_repo`, `_BRANCH_UNSAFE_RE` and `_malformed_api_ref` now refuse a malformed `repo`/`branch` before `_run` at all six call sites -- and the 20 parametrized cases across both files, still asserting the pre-guard call counts, went red on every `pytest` leg. Both files are repointed at the guarded state: the assertions invert to `len(calls) == 0`, the module docstrings cite `claude-oss#1035` and the 0.26.0 scaffold rather than the accepted-gap rationale, and `_latest_release`'s own coverage is merged into one parametrized case per value in `MALFORMED_REPO`, since `_malformed_repo` alone now refuses every one of them -- the old split between "refused by a pre-existing falsy check" and "reaches the API call" no longer exists. Every existing must-fire positive control is untouched.
