- Extended the #810 object-store-race retry to `tests/test_env_knob_parsing_654.py`'s own `_run_trail()` helper, which drives the same `git log -S` pickaxe walk `tests/test_git_trail_635.py` was already retrying past — the issue's own comment thread records two real recurrences in this file after the first fix landed. A non-zero exit whose stdout names an unreadable object gets one retry; anything else, including a genuine `trail.py` bug, fails on the first attempt.
