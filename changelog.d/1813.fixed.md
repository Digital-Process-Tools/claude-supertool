- **Corrected `test_description_is_not_a_changelog_1774.py`'s docstring, which justified its byte ratchet with a release-out-of-date claim** ([#1813](https://github.com/Digital-Process-Tools/claude-supertool/issues/1813)). It said `description` is "rendered whole by `ops`, so its length is a per-session tax" -- false since #1775/#1778 moved descriptions out of the default `ops` listing (measured at 3,721 bytes, signatures only). The ratchet itself is unaffected: `ops:full` and `help:OP` still render `description` verbatim on every call, and `op_help()` has no `compact`/`full` distinction of its own, so there is still no cheaper long form to move prose into -- the docs page for that op's family is. `docs/contributing.md` carried the same stale claim (`gh-prs` figures from 0.46.0) and is corrected alongside it. `tests/test_ratchet_reason_matches_ops_being_signatures_only_1813.py` pins the corrected claim against the actual render rather than trusting a hand-check of the prose.
