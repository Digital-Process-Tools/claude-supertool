- **`html-check`'s own test harness closed two of the three gaps #1296 left open, and refused the third with evidence** ([#1306](https://github.com/Digital-Process-Tools/claude-supertool/issues/1306)). Part 1: `tests/test_html_check.py::_run` now spawns the adapter with an outer `adapter_budget(ADAPTER)` timeout, matching every other adapter test file in the suite -- it had none, and was harmless only because the adapter's own 30s internal budget always fired first. Part 3: `errors` is now documented, in the adapter itself, as document order (ascending by the source line of the block that produced each entry) rather than left implicit for every `errors[0]` assertion to assume -- `test_two_real_findings_come_back_in_document_order` pins it. Part 2 needed no change: #1709 (2026-08-14, before this issue was picked up) already made `stalled_at_its_own_wall`'s decline message name both counts ("N of its M reported errors are that wall") for exactly the mixed stall-plus-finding shape this issue described.
