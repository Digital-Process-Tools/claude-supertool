- **Escaped the untrusted `.supertool.json` manifest entry that `worktree:setup`'s "declared in BOTH link and copy" refusal, and `worktree:teardown`'s two "manifest entry no longer valid" refusals, rendered raw** ([#2434](https://github.com/Digital-Process-Tools/claude-supertool/issues/2434)). All three sites ran BEFORE `_common.validate_entry`'s newline check (setup_op.py) or AFTER `safe_join` had just rejected the entry for containing one (teardown_op.py), and rendered the config/manifest entry into the op's own stdout receipt without `!r` -- unlike the file's own correct sibling sites, which already do. An entry crafted with an embedded newline plus a line prefix-identical to a genuine outcome line (`  linked: ...`, `  removed link: ...`) could forge an extra receipt line at column 0, since `.supertool.json` in the worktree BEING PROVISIONED is routinely someone else's branch. Now rendered with `!r` at all three sites, matching the convention `setup_op.py:91`/`:227` already use. `tests/test_worktree_setup_teardown_532.py` gained a forgery test (with a paired positive control) for each of the two files.
