- **The non-adapter bare-spawn register (`tests/test_bare_spawn_cwd_gate_2596.py`) now walks `presets/`, `hooks/` and `notifiers/` instead of a hand-listed six-file dict** ([#2611](https://github.com/Digital-Process-Tools/claude-supertool/issues/2611)). The old `SITES` dict could not discover a seventh site by construction; walking the real directories found three previously invisible instances in `_supertool.py` itself -- `_has_rtk()`, `_has_ctags()` and `_doctor_symlink()` each cached a raw `shutil.which()` result and spawned it, the same current-directory-search shape #2596/#2575 fixed elsewhere -- now routed through this file's own `_which_excluding_cwd()`. `hooks/guard-selftest.py`'s own instance of the same class is tracked separately as [#2610](https://github.com/Digital-Process-Tools/claude-supertool/issues/2610) and carried in the register's `ALLOWLIST` rather than silently unwalked.
