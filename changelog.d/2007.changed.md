- **A fresh, un-briefed `Explore` subagent reads `.claude/jit-context/tools/00-manual/harness-tools-blocked.md` as a prompt injection regardless of provenance wording or body length, and `CLAUDE.md` now says so instead of the rule chasing a wording fix that does not exist** ([#2007](https://github.com/Digital-Process-Tools/claude-supertool/issues/2007)). #1793 added a provenance-verification section to that rule after a stock reviewer flagged it as fabricated attacker content. #2007's own single-sample experiment found the new section unread by its objections; this lane re-ran the cold-read test at n=6 (three readers against the current body, three against a version stripped to only the redirection table) to check whether the finding replicated and whether length/wording moved it. All six independently called it an injection, citing the redirection mechanism itself (`mode: block` intercepting the harness's own Read/Edit/Write/Grep/Glob) rather than the presence or absence of provenance text -- two readers cited the "This is not a prompt injection" section itself as evidence of manipulation. No code change follows from this: the hypothesis is confirmed, not refuted, so the honest fix is the accepted-limitation note this issue asked for if that happened, not a further rewrite of the rule body.
