- **Fixed a jit-context `block` rule that a documented idiom walked straight through** ([#1987](https://github.com/Digital-Process-Tools/claude-supertool/issues/1987)). `merged-is-not-ancestry.md`, `git-C-has-cwd.md`, `op-defaults-that-narrow.md` and `supertool-no-cut.md` all anchored an optional `rtk` prefix directly before the guarded command with `(rtk[[:space:]]+)?`, so `rtk proxy git branch --merged` -- the documented idiom for a captured or piped command -- put `proxy` between `rtk` and the guarded command and never anchored. Measured returning a wrong 15-branch list for `merged-is-not-ancestry`. Widened to `(rtk[[:space:]]+(proxy[[:space:]]+)?)?` in all four files, admitting only the `proxy` subcommand rather than an arbitrary wrapper, and re-derived `00-index.tsv` by hand rather than through `rebuild-tsv.sh` (its installed copy writes a 7-column row against this tree's 6-column schema). `tests/test_jit_merged_prs_not_merged_1977.py` gained a `rtk proxy git branch --merged` regression row; every existing `MUST_NOT_FIRE` row stays green.
