- `grep:@-`/`around:@-` no longer print the colon-split disclosure ("Use
  grep:@- with a `pattern` key if the split was meant to fall elsewhere")
  when the caller is already on that route -- the payload route takes
  `pattern` verbatim, so nothing was split and the note sent the caller back
  to the route they were already using. The positional-CLI route, which does
  split on ':', keeps the disclosure (#1825).
