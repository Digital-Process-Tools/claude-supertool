- `_sanitize.py::wrap()` and each preset's own `read.py::render()` (youtube, bluesky, devto, hashnode) now flatten an injection-scan hit before joining it into the printed `POSSIBLE INJECTION` warning line. `detect()` returns the matched substring itself, and one of its own patterns (`(?:^|\n)\s*system\s*:`) matches across an embedded newline in untrusted text -- unflattened, that put attacker-chosen text at the start of a second line, indistinguishable from a line the tool wrote itself. #2593 fixed this exact shape at one call site (`presets/youtube/status_since.py`); this closes the 8 remaining call sites across all 4 preset directories, including `presets/hashnode/read.py`, which the issue's own recon pass had not checked but carries the identical shape ([#2636](https://github.com/Digital-Process-Tools/claude-supertool/issues/2636)).
