- **`duration_report.py`'s and `tests.yml`'s "Suite duration report" comments described `.github/duration-baseline.json` as a committed file, and it isn't** ([#2277](https://github.com/Digital-Process-Tools/claude-supertool/issues/2277)). `git ls-files .github/` never listed it, so the comparison half of #2206's feature has been in its own `no-baseline` state on every run since it landed — handled correctly and loudly by the script (`load_baseline` returns `("absent", None)`, the report says so explicitly, `main` always returns 0), but the two comments kept asserting a file that does not exist. Both now describe the current state — no baseline is committed yet, so every run compares to nothing until one lands — rather than a design intent read as a fact. No behavior changed; `load_baseline`'s `absent` handling is untouched.
