- **`tests/test_git_no_optional_locks_other_sites_1945.py` joins `.oss.json`'s `labels.lane_coupling_allowlist`** ([#2447](https://github.com/Digital-Process-Tools/claude-supertool/issues/2447)). The chokepoint change allowlisted `tests/test_git_invocation_chokepoint_2447.py` and rewrote `oss-lane-patterns.md` to say **both** guards were allowlisted on the same ground -- an architectural fact a cross-lane assertion has to name both sides of. Only one of the two reached `.oss.json`, so `/oss:doctor` kept reporting the 1945 guard as a possible #1201-shaped coupling while this repo's own notes said in bold that it had been acknowledged. A claim true of the intent and false on disk, which is the shape this repository files most often: the note is not changed here, the config is made to match it.
