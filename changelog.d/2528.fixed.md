- **The two cross-cutting untrusted-render guard tests now scan `presets/watch/ratelimit.py`** ([#2528](https://github.com/Digital-Process-Tools/claude-supertool/issues/2528)). `ratelimit.py` renders a poller's own error/id text through `_untrusted.flat()`, same as every other row on the `watches` board, but `tests/test_column_zero_renders_1522.py` and `tests/test_forged_branch_line_965.py` never scanned it -- a future regression there had no automated guard. There was no live defect (a same-PR regression test already confirmed the render was correct); the gap was that a *future* one would go uncaught.
