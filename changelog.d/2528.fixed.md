- **One of the two cross-cutting untrusted-render guard tests now scans `presets/watch/ratelimit.py`** ([#2528](https://github.com/Digital-Process-Tools/claude-supertool/issues/2528)). `ratelimit.py` renders a poller's own error/id text through `_untrusted.flat()`, same as every other row on the `watches` board, but neither `tests/test_column_zero_renders_1522.py` nor `tests/test_forged_branch_line_965.py` scanned it -- a future regression there had no automated guard. `test_column_zero_renders_1522.py` gained a new stanza covering it; `test_forged_branch_line_965.py` was deliberately left alone, since its scan is keyed to branch-refname-shaped dict keys and would find nothing in `ratelimit.py` regardless of whether the flattening broke, which is not a guard, only the appearance of one. There was no live defect (a same-PR regression test already confirmed the render was correct); the gap was that a *future* one would go uncaught.
