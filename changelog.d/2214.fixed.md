- **The always-injected jit note said the tools index has 6 columns; it has 7** ([#2214](https://github.com/Digital-Process-Tools/claude-supertool/issues/2214)). `rebuild-tsv.sh` has written a `requires` column since `claude-jit-context` 0.6.0 and `pre-tool-hook.sh` reads it as `tf[7]`, which `CLAUDE.md` recorded in #1992 and this rule did not. A short row reads as empty fields in the awk that consumes it, so the wrong count was never a visible error -- it arrives later as a silent widening in an unrelated diff, which is the defect the rule itself is about.
