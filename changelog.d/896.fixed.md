- **Two low-severity containment edges documented as accepted risk, and one docstring overclaim corrected** ([#896](https://github.com/Digital-Process-Tools/claude-supertool/issues/896)). F2: `_resolve_at_path` never containment-checks the `@file` reference itself, so `grep:@/tmp/outside.toml` loads and parses a file outside the project root -- verified not a content-disclosure channel (any path-shaped field read out of it is re-gated independently, and a TOML/JSON parse error reports only a line/column position, never the file's own text), so it is an existence-and-parseability oracle for paths outside cwd, not more, and is now documented on `_resolve_at_path` rather than gated. F3: `_containment_error` realpaths each candidate once, and the op that follows re-resolves the same string independently -- a classic TOCTOU window that predates this diff and is the shape of every "check, then act" gate backed by two separate syscalls; documented on `_containment_error` rather than closed, since closing it for real means opening every op's own file through an `O_NOFOLLOW`-style handle rather than a path string, out of proportion to a narrow-window race needing a second local account. F4: `presets/_untrusted.py`'s docstring claimed `_is_control` covers "exactly, in both directions" the ten code points `str.splitlines()` breaks on; a full 0x0-0x10FFFF sweep shows it is a strict superset by 57 code points (the rest of C0, all of C1, DEL) -- the safe direction, no ordinary path affected, docstring corrected and pinned by a new test asserting both counts.
