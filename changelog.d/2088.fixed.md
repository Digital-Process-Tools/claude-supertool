- **Shell, XML and four of seven jit-context indexes had no validator** ([#2088](https://github.com/Digital-Process-Tools/claude-supertool/issues/2088)). `shellcheck` and `bash-check` now run on `*.{sh,bash}` and on `.githooks/*`, which is where the extensionless `pre-commit` hook lives; `xmllint` runs on `*.xml`. Six tracked shell files had nothing checking them on write, `hooks/pre-bash-guard.sh` — the raw-command guard this repository gates itself with — among them. Both shell adapters are registered rather than one: `shellcheck` reports `skipped` when its binary is absent, and `bash-check` is `bash -n`, so the syntax question still gets an answer on a machine without it.

  `jit-index` matched `*00-manual/00-index.tsv`, which reached three of the seven `.tsv` files under `.claude/jit-context/` and left both `01-oss` indexes and both `01-paths.tsv` unread. A rule indexed in a layer no validator checks is the dead escape `tests/test_jit_index_dead_match_1254.py` exists to refuse, with the check removed rather than fooled. The glob is now `*jit-context/*.tsv`, verified by hand against all four previously-unreached files first: two validate clean and two decline with a reason, so nothing produces a false failure under `rollback_on_fail: true`.

  That test asserted the old glob as a literal string, with no docstring saying why, so widening the coverage reddened the suite. It now derives the population from the tree with `fnmatch` — the same function the core matches with (`_supertool.py:_match_glob`) — so widening passes and narrowing back is the red.

  `doctor:probe` goes from 12 validators configured and 8 resolving to 17 and 13, with none absent.
