- **`_configured` in `presets/watch/channel.py` now reads a pending-approval MCP server the same as a rejected one** ([#2247](https://github.com/Digital-Process-Tools/claude-supertool/issues/2247)). `claude mcp get` exits 0 for a third status this reader had no case for -- a project-scope `.mcp.json` server nobody has approved yet, printing `Status: ⏸ Pending approval` -- and it read as `True`, an established server, exactly like `#2208` before it fixed `Rejected`. `CLAUDE_REJECTED_STATUS_RE` is renamed `CLAUDE_NOT_LOADED_STATUS_RE` and now matches either status, so a pending-approval tag no longer opens the `#2182` census gate off a server the harness never actually loaded.
