- **Redacted the maintainer's absolute home path out of three `trap.d/` fragments, and added the regression guard `#2279` shipped without** ([#2426](https://github.com/Digital-Process-Tools/claude-supertool/issues/2426)). `trap.d/` ships whole into every plugin install (`.oss/statusline.py` reads it from the installed tree at runtime), and `#2279` fixed one instance of this by hand one release ago with no test added -- the same defect recurred: `trap.d/1165.pytest-heredoc-empty-stdin-runs-full-suite.md`, `trap.d/2015.scratchpad-collision.md` (three occurrences, one inside a scratchpad path) and `trap.d/2375.review-return-backref-false-positive.md` all carried `/Users/floriandavid/...`, redacted to `#2279`'s own `<worktree-NNNN>`/`<clone>` placeholder convention. `tests/test_trap_d_no_local_paths_2426.py` now globs every `trap.d/*.md` fragment on each run and refuses a literal `/Users/<name>/`, `/home/<name>/` or `C:\\Users\\<name>\\` path, while pinning the one legitimate lookalike already in the tree (`trap.d/1165`'s `C:\\Users\\runneradmin\\...` Windows-CI-runner example) as a case that must NOT trip the guard.
