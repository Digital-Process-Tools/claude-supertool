- **Corrected the last two stale `ops`/`ops-compact`/`ops:full` byte-count claims #1877 could not reach, plus five more stale `_HOOK_OUTPUT_CAP_BYTES` mentions self-review found in the same sweep** ([#1887](https://github.com/Digital-Process-Tools/claude-supertool/issues/1887)). Two of the four sites #1877 handed back as `report-for-filing` had already been fixed by unrelated, later commits (`_supertool.py:2575` by an `op_ops()` docstring rewrite, `_supertool.py:17101`'s `74,838` figure by a different correction) before this lane reached them, and `docs/contributing.md:122`'s figures already matched the render — except one clause in the same paragraph nobody had checked, which still said `_HOOK_OUTPUT_CAP_BYTES` `=` `7,168` (#2029 corrected the constant to 10,000 in 2026-08-27 and missed this exact sentence). The one live instance among the four named sites, `tests/test_ops_roster_1231.py`'s module docstring, still stated the pre-#1774 pair (`ops` 47,254 bytes, `ops-compact` 9,067, cap ~7,168) as a present-tense fact; it is now a dated correction naming the actual figures (`ops` 4,710 bytes, `ops-compact` 18,510, cap 10,000). Self-review then found the identical stale `7,168`/`~7KB` literal surviving in four more places `tests/test_render_size_claims_1877.py`'s registry never reaches because they are `.py` prose rather than a graded doc claim: `_supertool.py`'s `op_ops()` docstring and `_ROSTER_LEGEND` (the latter rendered live in every `ops`/`ops:roster` call), `tests/test_description_is_not_a_changelog_1774.py`, and `tests/test_guard_scope_is_a_route_not_a_file_1671.py` — whose `_SESSION_CAP = 7168` module constant now derives from `supertool._HOOK_OUTPUT_CAP_BYTES` instead of copying the literal a second time. `tests/test_render_size_claims_1877.py`'s `UNCOVERED` registry, which named the three #1877 sites as ungraded gaps, is now empty.
