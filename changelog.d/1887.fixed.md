- **Corrected the last two stale `ops`/`ops-compact`/`ops:full` byte-count claims #1877 could not reach** ([#1887](https://github.com/Digital-Process-Tools/claude-supertool/issues/1887)). Two of the four sites #1877 handed back as `report-for-filing` had already been fixed by unrelated, later commits (`_supertool.py:2575` by an `op_ops()` docstring rewrite, `_supertool.py:17101`'s `74,838` figure by a different correction) before this lane reached them, and `docs/contributing.md:122`'s figures already matched the render. The one live instance, `tests/test_ops_roster_1231.py`'s module docstring, still stated the pre-#1774 pair (`ops` 47,254 bytes, `ops-compact` 9,067, cap ~7,168) as a present-tense fact; it is now a dated correction naming the actual figures (`ops` 4,710 bytes, `ops-compact` 18,510, cap 10,000). `tests/test_render_size_claims_1877.py`'s `UNCOVERED` registry, which named all three as ungraded gaps, is now empty.
