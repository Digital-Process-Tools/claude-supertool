- **`_invariant_census.py`'s platform-signal scan now catches an `os.sep`-filtered assertion that would only ever regress on a Windows leg -- the exact leg every `invariant`-marked file is deselected from on 11 of 12 CI legs** ([#2376](https://github.com/Digital-Process-Tools/claude-supertool/issues/2376)). A release audit over `_PLATFORM_SIGNAL` flagged seven tree-walking test files as possibly line-ending/`os.sep`-sensitive; reading each against its actual assertions (rather than its vocabulary) found six false positives -- `test_validators_splitlines_1486.py`, `test_preset_git_splitlines_register_1130.py`, `test_forged_child_stream_line_1475.py`, `test_paste_backup_1650.py`, `test_anchored_guards_1188.py` and `test_toml_basic_string_seam_2152.py` are all either in-memory string comparisons or static AST/text scans over other files' source, none of which any host OS's newline or path-separator translation can touch -- and one real gap: `test_registry_names_dispatch_1285.py::test_preset_glob_is_non_empty` filters with `c.startswith("presets" + os.sep)`, written correctly today but a future hardcoded-`/` regression on that line would only ever fail on Windows, which was exactly the leg deselecting the file. `_PLATFORM_SIGNAL` now also matches `os.sep` (distinct from the `os.pathsep` token already there), and `tests/test_invariant_platform_gap_2376.py` pins both the one genuine gap and the six audited false positives so this reading does not have to be redone by hand later.
