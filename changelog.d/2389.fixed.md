- **`tests/test_pyright.py` and `tests/test_mypy.py` now run end to end on the `coverage` CI leg instead of `skipif`-skipping on every leg of every workflow** ([#2389](https://github.com/Digital-Process-Tools/claude-supertool/issues/2389)). Neither `pyright` nor `mypy` was installed anywhere in `tests.yml`, `slow-tests.yml` or `changelog.yml`, so every assertion in those two files that exercises the adapters end to end -- including the argv-containment regression #2379 fixed and the `--` separator mypy's own adapter relies on -- skipped on every platform, every Python version, every run, and a skip renders identically to a genuine pass on any board that counts red legs. `tests.yml`'s `coverage` job now installs both (`pip install pyright mypy`) alongside its existing `markdownlint-cli` step, the same #2019 precedent: one ubuntu leg off the critical path of the twelve, and the job already runs the whole suite (`-m 'not benchmark'`), so a single install reaches every previously-skipped assertion without adding either tool to the 12-way matrix.
