- **CI now installs `markdownlint-cli`, so the #2012 findings ceiling is actually enforced instead of skipping on every leg** ([#2019](https://github.com/Digital-Process-Tools/claude-supertool/issues/2019)). No leg of `.github/workflows/tests.yml` had markdownlint on PATH, so all four measurement tests in `tests/test_markdownlint_noise_2012.py` skipped -- a skip renders identically to a pass on any board that counts red legs. Pinned to `markdownlint-cli@0.49.1` (the version this repo's own rule choices and adapter tests were measured against) as one step in the `coverage` job, which already runs the whole suite off the critical path of the twelve-leg matrix rather than adding a node toolchain to all twelve.
