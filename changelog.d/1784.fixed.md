- **The `path escapes cwd` refusal now names the one remedy that leaves no residue in either repository's config** ([#1784](https://github.com/Digital-Process-Tools/claude-supertool/issues/1784)). `_ALLOW_OUTSIDE_HINT` named only `SUPERTOOL_ALLOW_OUTSIDE_CWD=1` and `"allow_outside_cwd": true` -- both edits to the *diagnosed* repository's own config, for what is usually a one-off read of a file in another root. Both agents who filed this actually prefixed the call with `cwd:PATH` instead, which moves the boundary for that one call and leaves no trace in either project's `.supertool.json`. The refusal now names `cwd:PATH` first, ahead of the two config-widening knobs it already named. The other four sub-items in #1784 -- `edit`'s refusal naming `paste` when the target does not exist, `edit:@-`'s array-payload refusal stating the one-edit-per-payload rule, and the `around`/`around_line` and `between`/`read:START-END` recovery messages -- were independently reproduced against the current code and found already fixed, with no change needed.
