- `.supertool.json` now sets `"no_publish_confirm": true` (#1897), so `gh-pr-merge:N:squash` merges
  without a `|force` suffix. Every gate the op performs is unchanged — the check tally, the
  leg reconciliation, the base-moved warning, and the `state`/`mergedAt`/`mergeCommit`
  read-back all still run, and every refusal it makes still refuses. What is removed is the
  second typing of the intent, which cost a maintainer four hand-run merges on 2026-08-21
  after four pull requests had passed every gate.

  The same switch reaches `devto_publish` and `bluesky_publish`, whose presets this project
  does not load. **Enabling either of those presets later means enabling it with
  confirmation already off**, and nothing announces that. Delete the line to restore the gate.
