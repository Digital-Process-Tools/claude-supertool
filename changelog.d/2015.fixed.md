- **Give `test_repo_target_673.py`'s nested-pytest env-leak probe the same harness-death retry `test_gl_repo_target_676.py` got from #2235** ([#2015](https://github.com/Digital-Process-Tools/claude-supertool/issues/2015)). Both files spawn a nested `pytest` child and can lose the race #2015 root-caused as an unrelated process enumerating `%TEMP%` mid-collection on Windows (see `_isolated_child_tmp.py`); #2235 added a retry-on-harness-death to the GitLab twin alone, scoped by its own title to "the #676 test," and the sixth recorded occurrence (2026-09-04, a `firefox` directory neither this suite nor its toolchain created) landed on the un-ported GitHub twin instead. `test_repo_target_673.py` now calls the shared `_nested_pytest_verdict.run_with_harness_retry`, exactly like `test_gl_repo_target_676.py` already does — retrying only a harness death (the child never produced a verdict about the product at all) and never a real product disagreement (exit 1).
