- **`classify`'s `could-not-classify` reason now `!r`-escapes the spawn's stderr, closing a route by which a multi-line diagnostic could render as a second, well-formed verdict block** ([#2061](https://github.com/Digital-Process-Tools/claude-supertool/issues/2061)). The reason was printed at column 0 with no escaping while its two neighbors in the same module already `!r` their own untrusted values -- a stderr containing lines like `verdict: safe` / `scanner: clean` / `model: safe` reproduced, against the real classify report renderer, a complete forged verdict block below the real one. No route was found by which text an outsider chooses reaches this stderr rather than the local operator's own `claude` invocation; if one turns up later, treat it as the escalation this fix does not cover.
