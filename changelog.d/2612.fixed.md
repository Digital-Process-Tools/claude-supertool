- `watches`/`radar`'s desktop-notification disclosure now distinguishes "will fire" from "opted in but `osascript` did not resolve" -- previously it read ON whenever `SUPERTOOL_WATCH_DESKTOP` was set, even when `desktop_notify` itself would silently no-op because `osascript` was absent or excluded by the cwd guard (#2612).
