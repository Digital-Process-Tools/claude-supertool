- `watches`/`radar`'s desktop-notification disclosure now distinguishes "will fire" from "opted in but it won't" -- previously it read ON whenever `SUPERTOOL_WATCH_DESKTOP` was set, even when `desktop_notify` itself would silently no-op because the platform has no `osascript` equivalent, or because `osascript` was absent or excluded by the cwd guard (#2612).
