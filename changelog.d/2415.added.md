- **`gh-issue-create` gained `dry_run` — a boolean payload key that runs parse, key/alias resolution, repo resolution and field validation exactly as a real call would, previews the labels/assignees/milestone/body/disclosure state that would be sent, and stops before either transport (the GraphQL `gh issue create` subprocess or the #1790 REST fallback) is ever invoked** ([#2415](https://github.com/Digital-Process-Tools/claude-supertool/issues/2415)). Verifying the op used to cost a real issue every time — #2407, #2410, #2413 were each filed and immediately closed in one session just to confirm the op still worked. `gh-pr-create`, `gh-pr-edit`, `gh-issue-comment` and `gl-issue-create` are candidates for the same key but were left for follow-up issues rather than smearing one lane's TDD coverage across all five.
