- **`channel:health`'s subscription block now says the #2182 census line is about a different server than the session tag** ([#2479](https://github.com/Digital-Process-Tools/claude-supertool/issues/2479)). It rendered `subscribed — session pid N carries server:plugin:supertool:claude-channel, and the harness has a server configured under that name` immediately followed by `the harness has no claude-channel server configured`: two true sentences about two different names, printed one qualified and one bare, with nothing marking them apart. Read at speed on 2026-09-09 by a session that had `presets/watch/channel.py` open, that is one server reported configured and unconfigured two lines apart, and it was relayed to the maintainer as a defect in the tool. The census branch is only reachable when the tag name differs from `CONSUMER_SERVER` (the `standing` gate), so the names differ by construction and the line now says so with no new gate: `separately, no server named claude-channel is configured -- a different name from the tag above`.
