- **Fixed a wrong reason in the comment above `_PATH_ARG_POSITIONS["grep_around"]`** ([#1842](https://github.com/Digital-Process-Tools/claude-supertool/issues/1842)). The comment explained the `(2,)` entry as "because its trailing slots must parse as ints, so a `:` in the pattern fails the call rather than moving the path" -- true of the three-and-four-token shapes, false of the two-token one: `grep_around:PATTERN:PATH` has no trailing int slot to fail on, so a `:` in the pattern moves the path exactly as `grep` does, and the call answers rather than failing. The table entry itself was already correct (slot 2 is the path in every shape); only the stated reason was wrong. The comment now names both shapes; `tests/test_grep_around_comment_1842.py` pins the two-token behaviour and greps the comment text so the two cannot drift apart silently again.
