- **A lane's own changed test files are now checked against the #418 encoding-seam guard locally, not only inside the full CI suite** ([#2287](https://github.com/Digital-Process-Tools/claude-supertool/issues/2287)). `tests/test_encoding_seam.py` enforces that rule tree-wide, but only inside the full pytest suite, so a violation used to be caught only after a lane had already pushed and burned a full CI leg -- routinely 9-10 minutes on Windows -- finding out. Measured 2026-09-04: fired 6 times across 6 lanes in one tick, every time on the lane's own brand-new test file. `validators/encoding-seam/encoding-seam.py` now runs the SAME scan functions, imported (never re-implemented) from `tests/test_encoding_seam.py`, as a supertool validator wired into `.supertool.json` -- it fires automatically on every `edit`/`paste` of a matching file, which every lane already goes through. `.github/scripts/check_encoding_seam.py` runs the identical check scoped to `git diff` against a base ref (or explicit files on argv), for a manual or CI pre-check pass. #2263 remains the broader "run the tree-wide guards on a diff" design; this is the narrow, cheap version of just this one guard.
