- `edit:@-`'s refusal for an `edits`/`ops`-shaped payload, and `help:edit`, now
  state the one-edit-per-payload contract as a positive sentence and point at
  `batch:@file` for more than one edit -- previously only inferable from the
  unknown-field list after a refused round-trip (#1867).
