- **A second probe writer reopened #1981's race within hours; the guard is now a class guard rather than a pin on one function** ([#1998](https://github.com/Digital-Process-Tools/claude-supertool/issues/1998)). `tests/test_repo_target_673.py`'s `#1962` env-leak probe wrote a real `.py` file at `REPO_ROOT / "tests" / "..."` and held it for the length of a nested `pytest` subprocess -- the identical shape #1981 had already fixed once in `test_gl_repo_target_676.py`, added afterward, in a lane explicitly told to keep its own probe under `tmp_path`. #1981's own guard (`test_repo_walk_probe_location_1981.py`) is written against that one function and could not see a new writer in a different file. The probe now writes under `tmp_path`, and a new class guard (`tests/_write_guard.py`) patches `pathlib.Path.write_text`/`write_bytes` process-wide for the life of the suite, installed once from `conftest.py`'s `pytest_configure`: any write that would land a `.py` file inside `repo_python_files()`'s walk root now raises immediately, at the point of the write, regardless of which test or file performs it. A snapshot-based before/after fixture was considered and rejected -- the defect is a file created and deleted inside one test, so a before/after diff can see nothing at all; a static source scan was rejected too, since it cannot see a path built at runtime rather than as a literal.
