- **`/oss:doctor` runs clean: test-duration measurement is attested, the loop's own filing label is declared, and the oss-owned statusline is back on the plugin's copy** ([#2298](https://github.com/Digital-Process-Tools/claude-supertool/issues/2298)). Four warnings, four different shapes. `addopts` gained `--durations=25` beside the coverage flags it already carried, and `.oss.json` now says so. `labels.filed_by_loop` was undeclared, which is not cosmetic: `dispatch_rank.rank` answers `could-not-rank` for every issue on the board while it is missing, so the dispatch order was inert and every tick reported intake as `could-not-count`. The label exists now and is declared -- and because the issues open today do not carry it, the current backlog reads as human-filed until the loop labels its own filings. `.oss/statusline.py` was the one worth stopping on: it had drifted *ahead* of the plugin, carrying the API-interpolation guards from #2245, #2278 and #2281, so the ordinary remedy for that warning would have deleted three hardening fixes. Those are filed upstream as [claude-oss#1035](https://github.com/Digital-Process-Tools/claude-oss/issues/1035) and this repo re-scaffolds instead of keeping a fork of a file it does not own -- which also picks up the `sys.stdin is None` guard the plugin had and this copy did not. The divergence ran both ways, which is why forking loses either direction you pick.
