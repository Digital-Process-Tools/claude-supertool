- **Measured what `gh-job:ID:fail` actually surfaces about a red CI leg, against six real failed runs pulled from this repository's own history, and shipped the one row #521's own table calls a rule rather than a heuristic** ([#521](https://github.com/Digital-Process-Tools/claude-supertool/issues/521)). Every failure in the sample was pytest-shaped — this repository has no PHP/rector/PHPStan CI leg, so that half of the issue's original classification table has no instances to build against yet; `docs/ci-failure-classification.md` writes up what was found, including that a `coverage` leg's failure is usually a DERIVED report of the same root cause as the `pytest` leg it depends on, not an independent signal. `classify_unit_test_failure` (`presets/_job_argv.py`, shared by `gh-job`/`gl-job`) recognises pytest's own `FAILED <nodeid>` marker and returns `"MANUAL"` — the issue's explicit rule that a bot must never decide "is the test wrong or the code wrong" — with `None` (not a false "no failure") when the rule does not fire. Not wired into either op's own CLI dispatch in this lane; that is named as follow-up work in the doc.
