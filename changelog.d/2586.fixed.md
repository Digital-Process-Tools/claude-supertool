- **`coverage (floors + disclosure)` was red on `master` because the `#2012` residue check saw a new markdownlint finding it did not know about** ([#2586](https://github.com/Digital-Process-Tools/claude-supertool/issues/2586)). Commit `f0323829` (a `/oss:scaffold` re-run) rewrote `.claude/jit-context/paths/01-oss/changelog-fragments.md` from the `claude-oss` plugin's own template, which puts an indented example ahead of a fenced ` ```bash ` block in the same document -- MD046 ("consistent code block style") flags the second against the first. `tests/test_markdownlint_noise_2012.py`'s `KNOWN_RESIDUE` set now names this file, with the same shape of reason as its two existing entries: the file is scaffolded wholesale, so a local edit to the fenced block would be lost at the next `/oss:scaffold --apply` run, and the real fix belongs in the `claude-oss` template rather than here.
