- **`tests/test_git_no_optional_locks_other_sites_1945.py` is now in `.oss.json`'s `labels.lane_coupling_allowlist`** ([#2447](https://github.com/Digital-Process-Tools/claude-supertool/issues/2447)). It statically references `presets/dashboard/dashboard.py` (`lane-watch`) and `presets/github/pr_merge.py` (`lane-tracker-ops`), and `/oss:doctor`'s lane-coupling check reported that span as a possible #1201-shaped coupling. It was one, and it was left unacknowledged on purpose: both files carried a private `_git()` instead of the shared chokepoint. #2447 merged and both now route through `presets/_git_run.py`, so what is left is a whole-repo guard spanning lanes by design, which is what the allowlist is for. `.claude/jit-context/paths/00-manual/oss-lane-patterns.md` said in bold that this span was the one deliberately not allowlisted; that sentence was true until the merge and is corrected in the same change rather than left to rot.
