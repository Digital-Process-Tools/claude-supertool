- **`youtube_comment` and `youtube_auth` no longer print raw OAuth/API error bodies or the cached scope string.** `presets/youtube/comment.py`'s two `get_access_token()`/insert-time `except` arms and `presets/youtube/auth.py`'s `authorize()` error arm wrote `sys.stderr.write(f"ERROR: {e}\n")` straight from a Google-supplied body, and `auth.py`'s `youtube_auth:status` printed the cached `scope` field the same unescaped way -- a newline anywhere in either would have put the remainder at column 0 of the receipt, exactly the shape #227 already fixed at one sibling site (`comment.py:140`'s `verify()`). All four now go through `repr(safe_short(str(e), 300))`, matching that fixed site ([#2599](https://github.com/Digital-Process-Tools/claude-supertool/issues/2599)).
