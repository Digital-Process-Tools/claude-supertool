- **Recounted the refname scanner's own coverage claim, and pinned the count as a test rather than a sentence** ([#1089](https://github.com/Digital-Process-Tools/claude-supertool/issues/1089)). `#976` probed `tests/test_forged_branch_line_965.py` against fourteen print/return shapes and found one caught; measured again against the scanner as widened by `#1038`/`#1092`, it now catches four of the fourteen (subscript reads joined the caught set; the marker-anywhere-in-the-same-expression gap `#976` called out by name is still open). `tests/test_refname_scanner_shape_recount_1089.py` re-runs the same fourteen shapes and asserts the count directly, so the next widening -- or the next silent regression -- is a required edit there instead of a stale docstring. `test_no_preset_prints_a_refname_raw` also now prints its own boundary (`checked N files, M sink shapes, these trees`) on every run, clean or not.

  The one live instance `#1089` named -- `presets/github/pr_merge.py`'s zero-checks refusal offering a `gh-branch:{head}` command for a colon-carrying branch -- was already fixed by `#1092` (gated on `_refname.ordinary()`); this adds the regression test for the colon case specifically, which was not pinned before.
