- **A scoped pre-push check for the env-scrub and splitlines-register meta-guards, run alongside the existing encoding-seam one** ([#2263](https://github.com/Digital-Process-Tools/claude-supertool/issues/2263)). `.github/scripts/check_meta_guards.py` imports (never re-implements) the same scan logic `tests/test_handrolled_path_env_guard_1151.py` and `tests/test_preset_git_splitlines_register_1130.py` already enforce tree-wide, scoped to `git diff` or explicit files on argv -- so a lane's own new test/preset code is checked against these three guard families before the push that otherwise burns a full CI leg finding out. `python3 .github/scripts/check_meta_guards.py` with no arguments diffs against the merge-base with `origin/<default branch>`; see `docs/contributing.md` for the full usage and the three exit codes.

  Deliberately narrow: three MORE guard families surfaced on this issue's own thread the same day it was measured (preset-global-lifetimes, hint-register, state-reset) are still uncovered by any pre-push check, named here rather than silently left out.
