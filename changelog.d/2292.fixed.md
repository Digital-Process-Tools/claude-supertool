- **`help:radar` disclosed the default branch as a board member re-evaluated on every `radar` call and stopped there, reading exactly as "nothing stands between ticks"** ([#2292](https://github.com/Digital-Process-Tools/claude-supertool/issues/2292)). That was already the wrong sentence: `tiers/gh_prs.py`'s `default_branch_report` has spawned and healed a standing `gh-branch` poller for the default branch since [#2024](https://github.com/Digital-Process-Tools/claude-supertool/issues/2024), on the same footing as the sibling `github-pr-feed` poller the same paragraph already discloses a few words later — it just never said so for the branch poller, and #2292 was filed quoting the very sentence that was silent about it. The op description now discloses the standing poller and its issue number, and `docs/presets/watch.md`'s "The default branch is a board member" section gets a paragraph doing the same, so a reader (or an issue filed against the architecture) does not have to read the Python to learn that discovery between ticks is already covered. No code changed: the poller, the spawn/heal wiring and the warnings for a down/capped/blind/stray poller were all already there.
